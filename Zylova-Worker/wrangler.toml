name = "zylova-api-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# No KV, no Durable Objects needed — this is a pure proxy worker
# Your API key is stored as a secret (see README for how to add it)
